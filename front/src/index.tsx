import React from 'react';
import ReactDOM from 'react-dom';
import Router from './app/root/react/router.container';

ReactDOM.render(
  <React.StrictMode>
    <Router />
  </React.StrictMode>,
  document.getElementById('root')
);